#include "main.h"

/**
 * _abs - computes absolute value for an integer
 * @n: the int will check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
