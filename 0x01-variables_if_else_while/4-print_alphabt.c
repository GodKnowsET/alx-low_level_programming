#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main Prints lower case exept e&q.
 *
 * Return: 0 each time
 */
int main(void)
{
	int alpha;

	for (alpha = 97; alpha < 123; i++)
	{
		if (alpha != 101 && alpha != 113)
		{
			putchar(i);
		}
	}
	putchar('\n');
	return (0);
}
