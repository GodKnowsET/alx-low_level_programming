#include <stdio.h>

/**
 * main - Prints the numbers 0 to 9.
 *
 * Return: 0 eachtime (Success)
 */
int main(void)
{
	int zerotonine;

	for (zerotonine = 0; zerotonine < 10; zerotonine++)
	{
		printf("%d", zerotonine);
	}
	putchar('\n');
	return (0);
}
